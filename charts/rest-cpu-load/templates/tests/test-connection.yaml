apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rest-cpu-load.fullname" . }}-test-connection"
  labels:
    {{- include "rest-cpu-load.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rest-cpu-load.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
